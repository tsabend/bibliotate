<% current_user.taught_courses.each do |course| %>
 <div class="row">
  <div class="card">
    <h3><%= course.name %></h3>
    <%= render partial: '/users/teachers/all_students', locals: {students: course.users}  %>
  </div>
 </div>
<% end %>
