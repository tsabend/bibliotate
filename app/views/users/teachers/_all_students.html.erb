<ul>
  <% students.each do |student|  %>
   <li><img class="student_image" src="<%= student.photo %>"></img> <%= student.name %>. Latest comment: <%= student.comments.last.body %> on <%= student.comments.last.story %></li>
  <% end %>
</ul>
