<ul>
  <% students.each do |student|  %>
    <li>
      <img class="student_image" src="
         <%= student.photo %>">
      </img>
     <%= link_to "#{student.name}",  student %>
     </li>
  <% end %>
</ul>
