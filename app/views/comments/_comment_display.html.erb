<div class="popup">
  <h1>What are people saying?</h1>
  <ul data-comment="display">
    <% @comments.each do |comment| %>
      <li><%= comment.user.name %> says:
      <%= comment.body %></li>
    <% end %>
  </ul>
  <%= render partial: 'comment_form',  locals: {id: @sentence_id} %>
</div>
