	<%= link_to user_path(comment.user) do %>
		<img class="student_image img-circle" src="<%=comment.user.photo%>">
		<%= comment.user.name %> 
  <% end  %>
	<br>
	<br>
<%= render partial: '/comments/hashtag_rendering', locals: {body: comment.body} %>

